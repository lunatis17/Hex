using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pivot : MonoBehaviour
{
    public Vector2Int pivotVector;
    public Vector2Int[] linkedHexagons = new Vector2Int[3]; // Bağlı hücrelerin GridSystem'deki koordinatları (Index)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
